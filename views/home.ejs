<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>url shortner</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
                    
            <div class="col-lg-12 text-center">
                <h1>url shortner</h1>
            </div>

            <div class="row col-lg-12 mb-4">
                    <form class="row g-3 text-center" action="/create" method="POST">

                        <div class="d-flex gap-2 align-items-center">
                              <input placeholder="peast your long url" name="longurl"
                              type="text" class="form-control" id="inputPassword2"/>

                              <input type="submit" class="btn btn-primary" value="Submit"/>
                        </div>

                    </form>
            </div>

            <div class="row col-lg-12">
                <% for(let i=0; i< urlResult.length; i++) { %>
                <div class="col-lg-3">
                    <div class="card text-bg-secondary mb-3" style="max-width: 18rem;">
                        <div class="card-header"> Total Click : <%= urlResult[i].clickCount %></div>
                        <div class="card-body">
                          <h6 class="card-title">
                            <a target="_blank" href=" http://localhost:4000/<%= urlResult[i].shortUrl %>">
                                http://localhost:4000/<%= urlResult[i].shortUrl %></a>
                           </h6>
                          <p class="card-text">
                            <%= urlResult[i].longUrl.slice(0,30) %>
                          </p>
                          <a class="btn btn-danger btn-sm" href="/delete/<%= urlResult[i]._id %>"> Delete </a>
                        </div>
                      </div>
                </div>      
                <% } %>
            </div>
      
    </div>
</body>
</html>